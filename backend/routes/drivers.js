const express = require('express');
const admin = require('firebase-admin');
const router = express.Router();
const { authenticate } = require('../middleware/auth');
const db = admin.database();
router.use(authenticate);
async function requireAdmin(req,res,next){try{const user=(await db.ref(`users/${req.user.uid}`).get()).val();if(!user||(user.userType!=='admin'&&user.role!=='admin'))return res.status(403).json({error:'Acesso administrativo necessário.'});req.adminUser=user;next();}catch(e){return res.status(500).json({error:'Não foi possível validar o administrador.'});}}
function sanitizeApplication(a={}){return {...a,cpf:a.cpf?`${String(a.cpf).slice(0,3)}***${String(a.cpf).slice(-2)}`:'',driverLicense:a.driverLicense?`${String(a.driverLicense).slice(0,2)}***${String(a.driverLicense).slice(-2)}`:''};}
function applicationFromUser(uid,user={}){const p=user.driverProfile||{},v=p.vehicle||{},a=p.address||{},m=user.driverApplication||{};return{uid,fullName:p.fullName||user.fullName||user.name||m.fullName||'',email:user.email||m.email||'',phone:p.phone||user.phone||m.phone||'',cpf:p.cpf||m.cpf||'',driverLicense:p.driverLicense||m.driverLicense||'',licensePlate:v.licensePlate||m.licensePlate||'',vehicleModel:v.model||m.vehicleModel||'',vehicleColor:v.color||m.vehicleColor||'',vehicleYear:v.year||m.vehicleYear||'',address:a.address||m.address||'',city:a.city||m.city||'',state:a.state||m.state||'',documents:Array.isArray(m.documents)?m.documents:[],documentCount:Number(m.documentCount||(Array.isArray(m.documents)?m.documents.length:0)),status:m.status||user.driverApprovalStatus||'pending',submittedAt:m.submittedAt||user.driverRegisteredAt||user.createdAt||null,reviewedAt:m.reviewedAt||null,reviewedBy:m.reviewedBy||null,recoveredFromUser:true};}
router.post('/register',async(req,res)=>{try{const uid=req.user.uid,data=req.body||{},required=['fullName','email','phone','cpf','driverLicense','licensePlate','vehicleModel','vehicleColor','vehicleYear','address','city','state'];const missing=required.filter(f=>data[f]===undefined||data[f]===null||String(data[f]).trim()==='');if(missing.length)return res.status(400).json({error:`Preencha os campos obrigatórios: ${missing.join(', ')}`});const vehicleYear=Number(data.vehicleYear);if(!Number.isInteger(vehicleYear)||vehicleYear<2010||vehicleYear>new Date().getFullYear())return res.status(400).json({error:'Ano do veículo inválido.'});const documents=Array.isArray(data.documents)?data.documents.slice(0,10).map(d=>({name:String(d?.name||'').slice(0,150),type:String(d?.type||'').slice(0,100),size:Math.max(0,Number(d?.size)||0)})):[];if(documents.length<3)return res.status(400).json({error:'Envie pelo menos 3 documentos.'});const existingUser=(await db.ref(`users/${uid}`).get()).val();if(!existingUser)return res.status(404).json({error:'Usuário não encontrado.'});if(existingUser.driverApprovalStatus==='approved')return res.status(409).json({error:'Este motorista já está aprovado.'});const submittedAt=new Date().toISOString();const application={uid,fullName:String(data.fullName).trim().slice(0,120),email:String(data.email).trim().toLowerCase().slice(0,160),phone:String(data.phone).trim().slice(0,30),cpf:String(data.cpf).trim().slice(0,20),driverLicense:String(data.driverLicense).trim().slice(0,30),licensePlate:String(data.licensePlate).trim().toUpperCase().slice(0,10),vehicleModel:String(data.vehicleModel).trim().slice(0,80),vehicleColor:String(data.vehicleColor).trim().slice(0,40),vehicleYear,address:String(data.address).trim().slice(0,180),city:String(data.city).trim().slice(0,80),state:String(data.state).trim().toUpperCase().slice(0,2),documents,documentCount:documents.length,status:'pending',submittedAt};await db.ref(`driverApplications/${uid}`).set(application);await db.ref(`users/${uid}`).update({userType:'driver',driverApprovalStatus:'pending',isOnline:false,driverRegisteredAt:existingUser.driverRegisteredAt||submittedAt,driverProfile:{fullName:application.fullName,phone:application.phone,cpf:application.cpf,driverLicense:application.driverLicense,vehicle:{licensePlate:application.licensePlate,model:application.vehicleModel,color:application.vehicleColor,year:application.vehicleYear},address:{address:application.address,city:application.city,state:application.state}},driverApplication:application});return res.status(201).json({success:true,application:{uid,status:'pending',submittedAt,documentCount:documents.length}});}catch(e){console.error('Erro ao registrar motorista:',e);return res.status(500).json({error:'Erro ao enviar cadastro de motorista.'});}});
router.get('/applications',requireAdmin,async(req,res)=>{try{const[a,u]=await Promise.all([db.ref('driverApplications').get(),db.ref('users').get()]),map=new Map();a.forEach(c=>map.set(c.key,sanitizeApplication(c.val()||{})));u.forEach(c=>{const user=c.val()||{};if(user.userType==='driver'&&!map.has(c.key))map.set(c.key,sanitizeApplication(applicationFromUser(c.key,user)));});const applications=Array.from(map.values()).sort((x,y)=>String(y.submittedAt||'').localeCompare(String(x.submittedAt||'')));return res.json({total:applications.length,applications});}catch(e){return res.status(500).json({error:'Erro ao listar cadastros de motoristas.'});}});
router.get('/online-count',requireAdmin,async(req,res)=>{try{const s=await db.ref('users').get();let online=0,approved=0;s.forEach(c=>{const d=c.val()||{};if(d.userType==='driver'&&d.driverApprovalStatus==='approved'){approved++;if(d.isOnline===true)online++;}});return res.json({online,approved});}catch(e){return res.status(500).json({error:'Erro ao consultar motoristas online.'});}});
router.patch('/:driverId/approval',requireAdmin,async(req,res)=>{try{const driverId=req.params.driverId,status=String(req.body?.status||'').toLowerCase();if(!['pending','approved','rejected'].includes(status))return res.status(400).json({error:'Status deve ser pending, approved ou rejected.'});const userRef=db.ref(`users/${driverId}`),appRef=db.ref(`driverApplications/${driverId}`),[us,as]=await Promise.all([userRef.get(),appRef.get()]);if(!us.exists()||us.val()?.userType!=='driver')return res.status(404).json({error:'Motorista não encontrado.'});const user=us.val(),existing=as.exists()?as.val():applicationFromUser(driverId,user),now=new Date().toISOString(),approved=status==='approved',updatedApplication={...existing,uid:driverId,status,reviewedAt:status==='pending'?null:now,reviewedBy:status==='pending'?null:req.user.uid,recoveredFromUser:!as.exists()},userUpdate={driverApprovalStatus:status,isOnline:false,driverApprovedAt:approved?now:null,driverApprovedBy:approved?req.user.uid:null,driverApplication:updatedApplication};await Promise.all([userRef.update(userUpdate),appRef.set(updatedApplication)]);return res.json({success:true,driverId,status,application:updatedApplication});}catch(e){return res.status(500).json({error:'Erro ao atualizar aprovação do motorista.'});}});
router.get('/available',async(req,res)=>{try{const lat=Number(req.query.lat),lng=Number(req.query.lng),radius=Number(req.query.radius??5);if(!Number.isFinite(lat)||!Number.isFinite(lng)||!Number.isFinite(radius)||radius<=0)return res.status(400).json({error:'Latitude, longitude e raio válidos são obrigatórios.'});const snap=await db.ref('users').orderByChild('userType').equalTo('driver').get(),drivers=[];snap.forEach(c=>{const d=c.val();if(d.driverApprovalStatus!=='approved'||!d.isOnline||!d.currentLocation)return;const a=Number(d.currentLocation.lat??d.currentLocation.latitude),b=Number(d.currentLocation.lng??d.currentLocation.longitude),dist=calculateDistance(lat,lng,a,b);if(Number.isFinite(dist)&&dist<=radius)drivers.push({uid:c.key,fullName:d.driverProfile?.fullName||d.fullName||'Motorista',ratingAverage:Number(d.ratingAverage??d.rating??0),ratingCount:Number(d.ratingCount||0),vehicle:{model:d.driverProfile?.vehicle?.model||'',color:d.driverProfile?.vehicle?.color||'',year:d.driverProfile?.vehicle?.year||null},location:{lat:a,lng:b},distance:Number(dist.toFixed(2))});});drivers.sort((a,b)=>a.distance-b.distance);return res.json({total:drivers.length,drivers});}catch(e){return res.status(500).json({error:'Erro ao listar motoristas.'});}});
router.get('/me',async(req,res)=>{try{const uid=req.user.uid,d=(await db.ref(`users/${uid}`).get()).val();if(!d||d.userType!=='driver')return res.status(403).json({error:'Usuário não é motorista.'});return res.json({success:true,driver:{uid,name:d.driverProfile?.fullName||d.fullName||d.name||d.email||'Motorista',email:d.email||'',status:d.driverApprovalStatus||'pending',isOnline:d.isOnline===true,currentLocation:d.currentLocation||null,vehicle:d.driverProfile?.vehicle||null,ratingAverage:Number(d.ratingAverage??d.rating??0),ratingCount:Number(d.ratingCount||0)}});}catch(e){return res.status(500).json({error:'Erro ao buscar dados do motorista.'});}});
router.post('/:driverId/status',async(req,res)=>{try{const{driverId}=req.params,{isOnline,currentLocation}=req.body;if(driverId!==req.user.uid)return res.status(403).json({error:'Você só pode alterar o próprio status.'});if(typeof isOnline!=='boolean')return res.status(400).json({error:'isOnline deve ser booleano.'});const ref=db.ref(`users/${driverId}`),d=(await ref.get()).val();if(!d||d.userType!=='driver')return res.status(403).json({error:'Usuário não é motorista.'});if(d.driverApprovalStatus!=='approved')return res.status(403).json({error:'Seu cadastro de motorista ainda não foi aprovado.'});const update={isOnline,lastLocationUpdate:new Date().toISOString()};if(currentLocation!==undefined){const lat=Number(currentLocation.lat??currentLocation.latitude),lng=Number(currentLocation.lng??currentLocation.longitude);if(!Number.isFinite(lat)||!Number.isFinite(lng))return res.status(400).json({error:'Localização inválida.'});update.currentLocation={lat,lng};await db.ref(`locations/${driverId}`).set({lat,lng,latitude:lat,longitude:lng,timestamp:new Date().toISOString()});}await ref.update(update);return res.json({message:'Status atualizado',isOnline});}catch(e){return res.status(500).json({error:'Erro ao atualizar status.'});}});
router.get('/:driverId',async(req,res)=>{try{const id=req.params.driverId,d=(await db.ref(`users/${id}`).get()).val();if(!d)return res.status(404).json({error:'Motorista não encontrado'});if(id===req.user.uid)return res.json(d);const s=await db.ref('rides').orderByChild('driverId').equalTo(id).get();let ok=false;s.forEach(c=>{const r=c.val();if(r?.userId===req.user.uid&&['SEARCHING','ACCEPTED','IN_PROGRESS'].includes(r.status))ok=true;});if(!ok)return res.status(403).json({error:'Acesso ao perfil do motorista não autorizado.'});return res.json({uid:id,fullName:d.driverProfile?.fullName||d.fullName||'Motorista',ratingAverage:Number(d.ratingAverage??d.rating??0),ratingCount:Number(d.ratingCount||0),vehicle:{model:d.driverProfile?.vehicle?.model||'',color:d.driverProfile?.vehicle?.color||'',year:d.driverProfile?.vehicle?.year||null},currentLocation:d.currentLocation||null});}catch(e){return res.status(500).json({error:'Erro ao buscar motorista.'});}});
router.post('/:driverId/rating',async(req,res)=>res.status(410).json({error:'Endpoint de avaliação antigo. Use POST /api/ratings.'}));
function calculateDistance(lat1,lon1,lat2,lon2){const R=6371,dLat=(lat2-lat1)*Math.PI/180,dLon=(lon2-lon1)*Math.PI/180,a=Math.sin(dLat/2)**2+Math.cos(lat1*Math.PI/180)*Math.cos(lat2*Math.PI/180)*Math.sin(dLon/2)**2;return R*2*Math.atan2(Math.sqrt(a),Math.sqrt(1-a));}
module.exports=router;
