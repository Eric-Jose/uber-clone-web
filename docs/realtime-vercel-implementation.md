PreçoFixo17 production realtime uses Vercel HTTP synchronization backed by Firebase. Socket.IO is optional and is not required for ride dispatch, acceptance, cancellation, or location state. Driver notifications are checked against the authoritative ride record before being returned, and stale notifications are removed. Concurrent driver acceptance remains protected by the Firebase transaction in backend/routes/rides.js.
