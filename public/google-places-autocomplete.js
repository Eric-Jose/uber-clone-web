/*
 * PreçoFixo17 map provider policy
 *
 * Google Maps / Google Places is intentionally disabled in this application.
 * All map screens use Leaflet with OpenStreetMap/CARTO tiles and OSRM routing.
 *
 * This legacy file is kept as a harmless compatibility stub so an old cached
 * page cannot initialize Google Places and trigger an API-key-required screen.
 */
(function () {
  'use strict';
  // Intentionally no-op. Do not load or initialize Google Maps/Places here.
})();
