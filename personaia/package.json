{
  "name": "personaia",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Assistente de IA pessoal - app Android (Capacitor) e PWA",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5173",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 4173",
    "cap:init": "cap init \"Personaia\" com.ericjose.personaia --web-dir=dist",
    "cap:add": "cap add android",
    "cap:sync": "npm run build && cap sync android",
    "apk": "npm run cap:sync && cd android && ./gradlew assembleDebug",
    "apk:release": "npm run cap:sync && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/cli": "^6.2.0",
    "@capacitor/core": "^6.2.0",
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.11"
  }
}
